export default {
    name: 'ViewRendering',
    template: `
        <div>
            <h2>View Rendering</h2>
            <p>See the <code>showcase.mvc.view</code> package for the @Controller code</p>
            <ul>
                <li><a v-on:click.stop.prevent="render" href="/view/html" class="textLink">HTML generated by Thymeleaf</a></li>
                <li><a v-on:click.stop.prevent="render" href="/view/viewName" class="textLink">DefaultRequestToViewNameTranslator convention</a></li>
                <li><a v-on:click.stop.prevent="render" href="/view/pathVariables/bar/pineapple" class="textLink">Using path variables in a view template</a></li>
                <li><a v-on:click.stop.prevent="render" href="/view/dataBinding/bar/orange" class="textLink">Data binding with URI variables</a></li>                
            </ul>
            <div class="alert alert-secondary mt-2" role="alert">
                <strong>Note</strong> Clicking the links render the view at the bottom.
            </div>
            <div class="embed-responsive embed-responsive-1by1" style="height: 200px;">
                <iframe v-if="screenVisibility" id="screen" src="/view/html" class="embed-responsive-item" scrolling="no"></iframe>
            </div>
            <div class="mb-4"></div>
        </div>	    
    `,
    methods: {
        render(event) {
            $('#screen').attr('src', $(event.target).attr('href'))
            this.screenVisibility = true
        }
    },
    data() {
        return {
            screenVisibility: false
        }
    }
}